input sentenceArgs {
  text: String!
  translation: String!
  desc: String!
}

input commentArgs {
  refWord: String!
  desc: String!
  word_fk: ID
}

input wordArgs {
  label: String
  meaning: String
}
